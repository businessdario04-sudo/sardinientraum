server.log
*.mp4
.claude/settings.local.json

# Laufzeit-Daten nie ins Git (Gäste-Daten, Passwort-Hash, TOTP-Secret)
data/auth.json
data/inquiries.json
data/availability.json
data/logs/
data/snapshots/

# Uploads landen nicht im Git (zu groß, dynamisch)
uploads/*
!uploads/.gitkeep
!uploads/logo.svg

# Sandbox-Inhalt nicht versionieren (nur Struktur)
sandbox/*
!sandbox/.gitkeep

# Auto-Backups vom Server
index.html.backup-*.html
index.html.bak
index.draft.html
